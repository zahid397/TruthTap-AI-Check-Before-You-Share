# TruthTap AI runs 100% on rule-based logic - NO API keys are required.
# The variables below are OPTIONAL future upgrades only.

# App URL (used for metadata / sharing)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# OPTIONAL: plug in an AI provider later for deeper analysis.
# The app works fully without these. Leave them empty for the hackathon MVP.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
